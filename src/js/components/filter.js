// ==========================================================================
// Компонент фильтровки
// ==========================================================================

class Filter extends Element {

    constructor(btn, globalClasses, hideClass) {
        super(globalClasses);
    }

}